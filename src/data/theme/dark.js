import { COLORS } from "../config/constants"

const theme = {
  palette: {
    type: "dark",
    primary: {
      main: COLORS.PRIMARY,
    },
    secondary: {
      main: COLORS.SECONDARY,
    },
  },
}

export default theme
