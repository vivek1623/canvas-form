import { COLORS } from '../config/constants'

const theme = {
  palette: {
    primary: {
      main: COLORS.PRIMARY
    },
    secondary: {
      main: COLORS.SECONDARY
    },
  }
}

export default theme;
